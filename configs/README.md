# Configurations

Locked, public-facing analysis configurations will be stored here.

Configuration files should be immutable within a tagged release. If a configuration changes after release, the new state belongs to a new release version rather than silently replacing the archived configuration.
